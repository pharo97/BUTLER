import SwiftUI

/// ─── VISUAL PLACEHOLDER ────────────────────────────────────────────────────
/// The pulse visualization has been cleared for a full redesign.
/// This stub keeps the project compiling while the new visual is designed.
/// Replace the body of PulseView with the new implementation.
/// ───────────────────────────────────────────────────────────────────────────
struct PulseWebView: View {

    let state:     String
    let amplitude: Double
    var birthPhase:  String? = nil
    var isSpeaking:  Bool    = false

    var body: some View {
        // Blank canvas — awaiting new visual design
        Color.clear
    }
}
