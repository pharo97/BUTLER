import SwiftUI

/// BIRTH ORB VISUAL — PLACEHOLDER awaiting new design.
/// This stub satisfies the Xcode project reference.
/// Replace with the new birth-phase visual when ready.
struct BirthOrbView: View {
    var phase: BirthPhase
    var isSpeaking: Bool

    var body: some View {
        Color.clear
    }
}
